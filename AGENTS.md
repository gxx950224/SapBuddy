# AGENTS.md — 文件路由与约束速查

## 文件路由表

| 需要…… | 读这个文件 |
|--------|-----------|
| 身份、铁律、编码规范、工具约束、输出约定 | SYSTEM.md |
| 报表开发完整流程 | .pi/prompts/abap-report.md |
| 函数模块开发完整流程 | .pi/prompts/abap-function.md |
| 代码审查流程（用户说 审查/审计 XXX） | .pi/prompts/abap-code-review.md |
| 程序逻辑详解（用户说解释/详解 XXX） | .pi/prompts/abap-explain.md |
| 打开网页版开发工具（Web IDE） | .pi/prompts/abap-webide.md |
| SAP 工具用法与标准工作流 | .pi/skills/gxx-abap/SKILL.md |
| gxx-abap CLI 命令参数详情 | .pi/skills/gxx-abap/references/commands.md |
| 报表代码模板与最佳实践 | .pi/skills/abap-report/SKILL.md |
| 报表开发规范（三段式 ALV、宏、常量） | .pi/skills/abap-report/references/report-standard.md |
| 函数模块代码模板与最佳实践 | .pi/skills/abap-function/SKILL.md |
| 代码审查清单（12 维度） | .pi/skills/abap-code-review/references/checklist.md |
| 通用 ABAP 语法、版本兼容性、RAP/CDS/OO 深入参考 | .pi/skills/sap-abap-skills/SKILL.md（29 个专题见其 
eferences/） |
| 历史踩坑记录 | Memory.md |

## 关键约束

工具约束、编码规范、安全规则详见 `SYSTEM.md`（工具约束速记一节）。

## 工作流程纲要

详细步骤见各 prompt 文件，纲要如下：

**报表开发**：需求分析 → 用户提供程序名/包名 →  `ls` 查重 →  `meta` 查表结构 → 输出逻辑说明（用户确认）→ 生成代码并展示（用户确认）→  `create` →  `put` →  `activate`

**函数模块开发**：需求分析 → 用户提供函数模块名 →  `ls` 查用户已在SE37创建好 →  `cat` 读取接口参数 →  `meta` 查出入参字段 → 输出逻辑说明（用户确认）→ 生成代码并展示（用户确认）→  `put` →  `activate`

**代码审查**：用户说审查/审计 XXX → 直接触发 abap-code-review skill → MCP 下载源码 → 静态走查 → 生成 HTML 报告

**程序逻辑详解**：用户说解释/详解 XXX → 直接触发 abap-explain skill → MCP 下载源码 → 解析逻辑 → 业务语言输出

## 通用要求（所有流程适用）

- **署名**：所有生成的代码文件、HTML 报告、输出文件，必须添加 "Generated by AbapBuddy" 签名。
- **自我检查**：每次完成前，回顾 SYSTEM.md 的铁律和编码规范，确保输出质量。
- **问题记录**：若遇到 Bug 并解决，自动记录到 Memory.md（由 safety.ts 自动完成，Agent 无需手动操作）。

### 环境与工具红线

1. **禁止用 bash 命令（ls、dir、find、test -f 等）检查文件或目录是否存在。**
   Write/Read 工具本身就有错误反馈，写失败或读失败自然会报错，不需要额外验证。

2. **文件路径统一用正斜杠 + 绝对路径**，格式：`D:/AbapBuddy/resources/output/xxx`。
   禁止混用反斜杠（`D:\xxx`）或 WSL 挂载路径（`/mnt/xxx`）。

3. **同一问题最多尝试 2 次。** 第一次失败后分析原因再试一次，第二次还失败就停手，报告用户。
   禁止用"换个写法"的方式反复重试（比如 ls 不行换 cmd /c dir，再不行换 find）。

4. **不要主动用 cmd.exe 执行命令**，优先用项目自带的工具（Read、Write、MCP）。
